# Point this at the FastAPI backend. "localhost" only works when the
# simulator/emulator runs on the same machine as the backend - when testing
# on a physical device (needed for LiveKit calls anyway), use your machine's
# LAN IP, e.g. http://192.168.1.23:8000
EXPO_PUBLIC_API_URL=http://localhost:8000
